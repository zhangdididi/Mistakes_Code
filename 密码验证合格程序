import java.util.Scanner;

public class Main{
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        while (in.hasNext()) {
            String str = in.nextLine();
            //保存类型数量
            int TypeCount = 0;
            boolean flag = (str.length() > 8);
            char[] chars = str.toCharArray();
            for (int i = 0; i < chars.length && flag; i++) {
                
            }
        }
    }
}
